# napi-rs artefacts. Every file under `src/generated/` is regenerated by
# `pnpm build` (see `crates/hypr-napi/.napi-rs.json`), so keep the whole
# folder ignored and recreate it on demand.
src/generated/*
!src/generated/.gitkeep
