---
title: "CLI Reference"
section: "CLI"
description: "Command-line reference for the char CLI"
---

## char

Live transcription and audio tools

```
char [--base-url] [--api-key] [-m] [-l] [--no-color] [--base] [-v] [-q] [-h] [-V] <command>
```

**Global options:**

- `--base-url <BASE_URL>`
- `--api-key <API_KEY>`
- `-m, --model <MODEL>`
- `-l, --language <LANGUAGE>` (default: `en`)
- `--no-color`
- `--base <DIR>`
- `-v, --verbose` — Increase logging verbosity
- `-q, --quiet` — Decrease logging verbosity

**Options:**

- `-h, --help` — Print help
- `-V, --version` — Print version

**Subcommands:**

- `transcribe` — Transcribe an audio file
- `completions` — Generate shell completions

---

### char transcribe

Transcribe an audio file

```
char transcribe <--input> <-p> [-k] [-o] [-f] [-h] [-V]
```

**Options:**

- `--input <FILE>`
- `-p, --provider <PROVIDER>`
- `-k, --keyword <KEYWORD>`
- `-o, --output <FILE>`
- `-f, --format <FORMAT>` (default: `json`)
- `-h, --help` — Print help
- `-V, --version` — Print version

### char completions

Generate shell completions

```
char completions [-h] [-V] <shell>
```

**Options:**

- `-h, --help` — Print help
- `-V, --version` — Print version

**Arguments:**

- `<SHELL>`

---

## Standalone commands

The standalone CLI build (installed via `npm` or direct download) includes additional commands.

### char models

Manage local models

```
char models [-h] [-V] <command>
```

**Subcommands:**

- `paths` — Show resolved paths for settings and model storage
- `list` — List available models and their download status
- `cactus` — Manage downloadable Cactus models (Apple Silicon only)
- `download` — Download a model by name
- `delete` — Delete a downloaded model

### char models list

```
char models list [--kind] [--supported] [-f] [-h] [-V]
```

**Options:**

- `--kind <KIND>` — Filter by model kind (`stt` or `llm`)
- `--supported` — Only show supported models (requires `--kind stt`)
- `-f, --format <FORMAT>` (default: `json`)

### char record

Record audio to a WAV file

```
char record [--audio] [-o] [--sample-rate] [-h] [-V]
```

**Options:**

- `--audio <AUDIO>` — Audio source: `input` (mic), `output` (system audio), or `dual` (default: `input`)
- `-o, --output <FILE>` — Output file path (default: auto-generated with timestamp)
- `--sample-rate <RATE>` (default: `16000`)
- `-h, --help` — Print help
- `-V, --version` — Print version

Press Ctrl+C to stop recording.

### char desktop

Open the desktop app or download page.

### char bug

Report a bug on GitHub.

### char hello

Open char.com.

